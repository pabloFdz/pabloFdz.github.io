function n() {
  return {
    name: "astro-font-picker",
    hooks: {
      "astro:config:setup": ({ addDevToolbarApp: o }) => {
        const t = import.meta.url, r = t.substring(0, t.lastIndexOf("/"));
        o(r + "/astroFontPicker.js");
      }
    }
  };
}
export {
  n as default
};
